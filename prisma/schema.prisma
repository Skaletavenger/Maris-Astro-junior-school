generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
URL = ENV("postgres://0129d30c97850404cd04e65e8ba84ea6547a0769b2284bf8b505d047a046fffa:sk_PMwf-8TtBa93pp-nnqs3O@db.prisma.io:5432/postgres?sslmode=require")
}

model GalleryImage {
  id        String   @id @default(cuid())
  url       String
  name      String?
  createdAt DateTime @default(now())
}

model SchoolDocument {
  id        String   @id @default(cuid())
  url       String
  name      String
  createdAt DateTime @default(now())
}
